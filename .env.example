# Vercel Postgres — utwórz bazę w dashboardzie Vercel (Storage → Create Database → Postgres),
# połącz z projektem, a te zmienne zostaną wstrzyknięte automatycznie.
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=

# Klucz do Anthropic API — potrzebny do endpointu /api/ai-insight (Claude Haiku).
ANTHROPIC_API_KEY=
